# Python
__pycache__/
*.pyc
.venv/

# Biến môi trường chứa credential — KHÔNG commit
backend/.env
.env

# Database local (Render tự tạo lại từ create_all)
# backend/smarthome.db  # đang được commit sẵn 3 thiết bị — bỏ comment nếu muốn ngừng track
